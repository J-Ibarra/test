/**
 * The states that
 */
export enum WithdrawalState {
    cancelled = 'cancelled',
    completed = 'completed',
    holdingsTransactionCompleted = 'holdingsTransactionCompleted',
    pending = 'pending',
}